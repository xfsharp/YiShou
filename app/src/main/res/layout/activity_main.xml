<?xml version="1.0" encoding="utf-8"?>
<com.xf.yishou.views.MySlidingPaneLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activity.MainActivity"
    android:background="@android:color/white"
    android:clipToPadding="true"
    android:fitsSystemWindows="true"
    android:id="@+id/slid_pane">

    <!--菜单界面容器-->
    <RelativeLayout
        android:id="@+id/rl_menu"
        android:layout_width="180dp"
        android:layout_height="match_parent"
        android:gravity="center_horizontal">
    </RelativeLayout>

    <!--主界面容器-->
    <RelativeLayout
        android:id="@+id/rl_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <!--android:background="@android:color/holo_blue_light"-->

        <ImageView
            android:id="@+id/iv_menu_open"
            android:layout_width="40dp"
            android:layout_height="45dp"
            android:src="@mipmap/menu_icon"/>

        <TextView
            android:id="@+id/tv_page_tiele"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:gravity="center"
            android:textColor="@android:color/darker_gray"
            android:textSize="22sp"/>
        <!--android:textColor="@android:color/white"/>-->

        <!-- android:text="主界面"/>-->

        <FrameLayout
            android:id="@+id/fl_main"
            style="@style/style_liner_match"
            android:layout_alignParentBottom="true"
            android:layout_below="@id/tv_page_tiele">
        </FrameLayout>
    </RelativeLayout>
</com.xf.yishou.views.MySlidingPaneLayout>
